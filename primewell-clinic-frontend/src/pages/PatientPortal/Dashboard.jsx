const Dashboard = (props) => {
    return (
        <div>
            <h1>Dashboard</h1>
            <p>Welcome {props.First_Name} to the Dashboard!</p>
        </div>
    );
};

export default Dashboard;